<!-- Footer -->
<footer class="footer bg-primary text-white pt-8 rounded-tl-2xl  rounded-tr-2xl f-footer-area">
      <div class="max-w-7xl mx-auto px-4">
            <div class="flex flex-wrap justify-between">
          <!--         <div class="w-full md:w-1/2 mb-6 md:mb-0">
                        <h5 class="text-lg font-semibold mb-3">Contact Us</h5>
                        <p>Address: {{ optional($menuList['metaInfo'])['address'] ?? null }}</p>
                        <p>Phone: {{ optional($menuList['metaInfo'])['phone'] ?? null }}</p>
                        <p>Email: {{ optional($menuList['metaInfo'])['email'] ?? null }}</p>
                  </div> -->
<!--                   <div class="w-full md:w-1/3 md:text-right">
                        <h5 class="text-lg font-semibold mb-3">Important Links</h5>
                        <ul class="space-y-1 md:inline-block">
                              @foreach($menuList['linkList'] as $link)
                              <li><a href="{{$link->linkUrl}}" class="underline hover:underline">{{$link->title}}</a>
                              </li>
                              @endforeach
                        </ul>
                  </div> -->
            </div>
      </div>
      <div class="text-right py-2 mt-4 bg-primary-dark text-white text-sm">
            Developed by <strong>2RA Technology Ltd</strong> 
          <!--   {{ Date('Y') }} All right reserved -->
      </div>
</footer>